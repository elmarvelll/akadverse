// src/app/studashboard/main-menu/page.tsx
//
// Landing page for the "Main Menu" workspace card on
// src/app/studashboard/page.tsx. Not built out yet — Marketplace now lives
// as its own top-level workspace (src/app/studashboard/marketplace/)
// rather than nested under here, so this is just a placeholder for
// whatever else "Main Menu" ends up covering later.

import ComingSoon from "@/app/components/dashboard/shared/ComingSoon";

export default function MainMenuPage() {
  return <ComingSoon portalName="Main Menu" />;
}
