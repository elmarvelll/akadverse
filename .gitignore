# Environment / secrets
.env
.env.*
*.pem
*.key
secrets/

# Node / TypeScript
node_modules/
dist/
build/
.next/
out/
.turbo/
*.tsbuildinfo
coverage/
.pnpm-store/

# Generated Prisma clients (regenerated by postinstall/build — see
# prisma/elearning/schema.prisma's `generator client { output = ... }`)
src/generated/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Java
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# Elixir
_build/
deps/
*.beam
erl_crash.dump
.elixir_ls/

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log
.vercel
.env*

# local backups written by scripts/cleanup-test-accounts.ts (contain emails / password hashes)
.cleanup-backups/

# Environment templates contain only placeholders / local-only values and MUST be committed
# (the `.env*` rules above would otherwise ignore them). See for_Developers/.env.example.
!.env.example
